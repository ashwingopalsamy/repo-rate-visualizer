name: Validate RBI Data

on:
  push:
    branches: [main]
    paths:
      - ".github/workflows/**"
      - "requirements-hf-dataset.txt"
      - "scripts/**"
      - "public/data/**"
      - "src/data/snapshot.json"
      - "src/data/snapshotV2.js"
      - "src/data/supplementalHistory.js"
      - "hf-dataset/**"
      - "tests/**"
      - "package.json"
      - "package-lock.json"
  pull_request:
    paths:
      - ".github/workflows/**"
      - "requirements-hf-dataset.txt"
      - "scripts/**"
      - "public/data/**"
      - "src/data/snapshot.json"
      - "src/data/snapshotV2.js"
      - "src/data/supplementalHistory.js"
      - "hf-dataset/**"
      - "tests/**"
      - "package.json"
      - "package-lock.json"

concurrency:
  group: validate-rbi-data-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v7
        with:
          node-version: "20"
          cache: "npm"

      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"
          cache: "pip"
          cache-dependency-path: requirements-hf-dataset.txt

      - name: Install Node dependencies
        run: npm ci --no-audit --no-fund

      - name: Install Hugging Face dataset tooling
        run: python -m pip install --disable-pip-version-check -r requirements-hf-dataset.txt

      - name: Validate source data and dataset artifact
        run: |
          npm run test:data
          npm run test:hf-dataset
          npm run build
          git diff --check
